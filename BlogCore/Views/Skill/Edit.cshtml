@model SkillModel

@{
    Layout = "_ManagementLayout";
    ViewData["Controller"] = "Skills";
    ViewData["Action"] = "Edit";
    ViewData["Title"] = $"{ViewData["Controller"]} - {ViewData["Action"]}";
    ViewData["Icon"] = "fas fa-sparkles fa-fw";
}
<section id="skill-edit-form" class="form-component">
    <div class="header">
        <h2><i class="fas fa-sparkles fa-fw" aria-hidden="true"></i> Edit</h2>
    </div>
    <div class="body">
        <form v-on:submit.prevent="submit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="mb-3">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" v-model="name" class="form-control" />
                <span v-if="errors.name" class="text-danger">{{errors.name}}</span>
            </div>
            <div class="mb-3">
                <label asp-for="Proficiency" class="control-label"></label>
                <select asp-for="Proficiency" v-model="proficiency" class="form-control">
                    <option>Basic</option>
                    <option>Intermediate</option>
                    <option>Advanced</option>
                </select>
                <span v-if="errors.proficiency" class="text-danger">{{errors.proficiency}}</span>
            </div>
            <div class="mb-3">
                <input type="submit" value="Save" class="btn btn-primary bc-button" />
            </div>
            <div class="mb-3">
                <div v-if="errors.result" class="mb-3 alert alert-danger">
                    <i class="fas fa-exclamation-triangle fa-fw" aria-hidden="true"></i> {{errors.result}}
                </div>
            </div>
        </form>
        <a asp-action="Index"><i class="fal fa-arrow-left fa-fw" aria-hidden="true"></i> Back to List</a>
    </div>
</section>